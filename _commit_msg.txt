feat: 新增 MQTT 设备接入适配层与设备历史曲线

后端：
- 新增 server/mqtt-bridge.js：订阅 <前缀>/devices/<设备编号>/readings 与 /heartbeat，
  复用 devices.js 的同一套鉴权与入库逻辑，HTTP 与 MQTT 上报结果完全一致
- 新增 server/mqtt-broker.js：基于 aedes 的本地 broker，没有真实 broker 时用于演示联调
- devices.js 抽出 authenticateDevice 供 HTTP/MQTT 共用；新增 seriesForDevice 历史曲线聚合
- 新增 GET /api/devices/:id/series?hours=24&buckets=12（等长分桶的温度/湿度/CO₂/光照均值与极值）
- .env 增加 MQTT_URL / MQTT_TOPIC_PREFIX / MQTT_USERNAME / MQTT_PASSWORD / MQTT_BROKER_PORT
- 新增 npm run mqtt / mqtt:broker / test:mqtt

测试：
- 新增 server/test-mqtt.js 端到端测试：临时 broker → 适配层订阅 → 发布报文 →
  校验入库来源为 device、自动生成 3 条预警、心跳刷新在线、密钥错误丢弃不入库、曲线聚合
- 说明：本地 broker 依赖锁定 aedes@0.51.3（aedes v1 改为 ESM 异步工厂，兼容性差）

小程序：
- 监测页设备卡片支持展开「近 24 小时温度曲线」，显示分桶柱状图与平均/最低/最高、
  记录条数、分桶粒度；按需加载，不增加自动刷新的负担

文档：更新仓库首页、后台 README（MQTT 主题与报文约定、演示步骤、曲线接口）、小程序 README
