# 菇事云管家 · 小程序功能介绍书

**数字技术赋能赣北食用菌产业振兴**
以基地、批次、菌棒为核心档案，打通生产记录、环境监测预警、AI 问答、质量溯源与产销对接。

本文档逐一说明每个功能的用途、入口与规则，适合新用户上手与答辩讲解。

---

## 一、三种使用方式

| 形态 | 入口 | 说明 |
|---|---|---|
| 微信小程序 | 微信开发者工具导入 `gushi-miniapp` | 正式形态，支持扫码、图片上传等微信能力 |
| 浏览器 H5 预览 | `gushi-miniapp` 下 `npm run build:h5 && npm run preview:h5` → http://localhost:5173 | 界面与小程序一致，用于快速查看 |
| 网页版管理端 | 启动后台后访问 http://localhost:3000 | 零安装，手机/电脑浏览器都能用，功能与小程序对齐 |

三端共用同一个后台（Express + SQLite），数据互通、实时一致。

---

## 二、账号体系

### 1. 注册新账号
- 入口：登录页「注册新账号」/ 网页版登录页
- 需填写：账号（3-32 位字母、数字、下划线或中文）、密码（6-64 位，**全平台不可重复**）、称呼（可选）
- **注册身份点选**（芯片式，点选后实时显示该身份的权限说明）：
  - 菇农（普通用户）：可录入基地、批次、环境、溯源、任务、设备；供应与采购仅可浏览；不能回复提问
  - 合作社/基地管理员：业务数据全部可录，可发布供应与采购需求，可管理大棚设备；不能回复提问
  - 采购商：可发布采购需求；基地、批次、溯源、供应仅可浏览；不能回复提问
- **不可自选的身份**：政府/服务机构、专家、平台管理员——这三类只能由平台管理员分配，即使绕过界面传参也会被后端强制降为菇农

### 2. 登录
- 账号密码登录，或微信一键登录（需后台配置 `WX_APPID / WX_SECRET`）
- 登录态为 JWT，**7 天有效**；退出登录立即失效
- 所有密码输入框均有 **👁 显示/隐藏切换**（隐藏时闭眼图标、明文时睁眼图标）
- 每个账号的数据相互隔离：基地、批次、环境、溯源、供应、采购等记录仅本账号可见

### 3. 退出登录
- 首页「当前账号」卡片 / 网页版右上角 👉「退出登录」，退出后需重新登录

---

## 三、角色权限矩阵

| 能力 | 平台管理员 | 基地管理员 | 菇农（默认） | 专家 | 采购商 | 政府机构 |
|---|---|---|---|---|---|---|
| 基地/批次/环境/溯源/任务/设备录入 | ✅ | ✅ | ✅ | 🔒 只读 | 🔒 只读 | 🔒 只读 |
| 发布供应 | ✅ | ✅ | 🔒 只读 | 🔒 只读 | 🔒 只读 | 🔒 只读 |
| 发布采购需求 | ✅ | ✅ | 🔒 只读 | 🔒 只读 | ✅ | 🔒 只读 |
| 回复用户提问（专家回复） | ✅ | 🔒 | 🔒 | ✅ | 🔒 | 🔒 |
| 查看全部账号 / 分配职务 | ✅ 唯一 | 🔒 | 🔒 | 🔒 | 🔒 | 🔒 |
| 数据范围 | 全部模块 | 本账号 | 本账号 | 本账号 | 本账号 | 本账号 |

> 权限由后端强制校验（RBAC），越权一律返回 403；前端隐藏无权限入口只是体验优化。

---

## 四、功能模块详解（小程序端）

### 1. 首页（Tab 1）
- **今日数据**：生产基地、生产批次、监测设备（在线台数 + 自动上报条数）、待处理预警，全部来自后台数据库实时汇总，无数据时显示空状态，不编造数字
- **快捷入口**：新增基地 / 新增批次 / 大棚设备 / AI 问答
- **最新环境记录**：设备自动上报与手动补录的最近数据（区分“硬件自动 / 手动补录”标识）
- **待处理预警**：超阈值记录列表，可点击「处理」完成闭环
- **生产任务**：任务列表（含优先级、截止日期）
- **当前账号**：显示账号、角色与数据量，支持退出登录
- **账号管理 · 分配职务**（管理员可见）/ **角色权限说明**（所有人可查）

### 2. 生产（Tab 2）
- **生产批次台账**：批次编号、所属基地、菌种/品种、菌棒数量、当前阶段、入库日期、预计出菇、状态
- **新增批次 / 新增基地**：表单弹层录入，保存后写入 SQLite
- **任务优先级建议**：输入任务标题与截止日期，调用 AI（`/api/ai/suggest-priority`）给出高/中/低建议；未配置 AI 密钥时自动降级为规则引擎，并标注来源；可按建议一键生成任务
- **生产任务列表**：按优先级高亮展示

### 3. 监测（Tab 3）
- **接入大棚设备**：填写名称、所属基地与阈值（温度 ≤26℃、湿度 ≥80%、CO₂ ≤800ppm，可按设备单独调整），系统生成**设备编号 + 设备密钥**
- **设备自动上报**：把编号与密钥配置到温室网关（ESP32 等），设备定时上报温湿度、CO₂、光照；支持 HTTP（`POST /api/ingest/readings`）与 MQTT 两种接入
- **在线判定**：10 分钟内有上报视为在线，超时自动显示离线
- **查看设备密钥**：随时弹层查看编号/密钥及上报报文示例
- **近 24 小时曲线**：温度按时间分桶的柱状趋势图（平均值、最低、最高、记录条数）
- **手动补录**：断网或核对历史数据时手工录入环境记录，超阈值同样自动预警
- **待处理预警**：自动按阈值生成，可点击「处理」；页面每 30 秒自动刷新

### 4. 溯源（Tab 4）
- **按批次编号查询**：输入编号（如 `GUSHI-20260901-01`）或**扫码查询**，返回批次、基地、生产事件时间轴与该批次产品
- **新增溯源事件**：入库、接种、养菌、催蕾、出菇、采摘、质检、出库、运输等环节绑定到批次，含操作人与日期
- **溯源二维码**：消费者扫码验证产品来源，查询全程记录

### 5. 市场（Tab 5）
- **发布供应**：关联真实批次，填写产品名称、数量、单位、价格、可售日期与说明；支持预设图标或**上传自定义图片**
- **供应列表**：可供应/已下架状态、数量价格一览、**更换产品图标**
- **采购需求**：采购方、产品、数量、意向价、联系方式与具体要求
- 仅展示后台已保存的真实数据，空态明确提示

### 6. AI 问答
- 页面顶部提示当前 AI 接入状态（已接入大模型 / 规则知识库）
- 提问表单：问题标题、类型（病虫害防治/环境调控/生产流程/设备使用/市场销售）、关联基地（可选，AI 会结合该基地最近批次与环境数据作答）、详细描述
- **提交即自动作答**：走大模型（`deepseek-chat` 等兼容接口），无密钥或调用失败自动降级为内置规则知识库，并标注回答来源（AI 大模型 / 规则知识库 / 人工专家）
- **问答记录**：每次提问都存档，显示回答来源与时间，便于专家复核；专家与平台管理员可人工补充回复

### 7. 登录页
- 登录 / 注册双 Tab、密码显隐切换、微信一键登录、演示账号提示

---

## 五、网页版管理端（3000 端口）独有增强

在浏览器打开后台地址即可使用，与小程序同一套账号和数据：

- **注册**：身份芯片点选（同上，排除政府/专家/管理员）+ 密码显隐
- **职位分配**（导航栏「职位分配」按钮，登录后可见）：
  - 管理员点击直接进入；非管理员弹出**管理员登录**，验证通过自动切换并进入
  - **方式一**：账号列表中**点击任意账号的职位徽章**，展开六种职位芯片，点选后确认即修改
  - **方式二**：输入**目标账号 + 目标账号密码**核验后直接分配
  - 修改均有「职位分配提醒」确认弹窗；对方重新登录后生效
- **删除功能**：批次、环境数据、溯源事件、供应、采购需求、问答记录、生产任务的每条记录都有红色「删除」按钮，点击弹出「删除提醒：确定删除这条记录吗？删除后不可恢复。」确认后删除（只能删本账号记录）
- **专家问答**：提问表单直接内嵌在页面主体（标题/类型/关联基地/描述），提交后 AI 立即作答，不再藏在按钮里
- 登录界面隐藏 👤/＋ 等操作按钮，登录后恢复

---

## 六、主要接口一览

| 模块 | 接口 | 说明 |
|---|---|---|
| 账号 | `POST /api/auth/register` | 注册（role 仅限菇农/基地管理员/采购商） |
| | `POST /api/auth/login` / `POST /api/auth/logout` / `GET /api/auth/me` | 登录 / 退出 / 当前用户 |
| 管理员 | `GET /api/admin/users`、`PUT /api/admin/users/:id/role`、`POST /api/admin/users/assign` | 账号列表 / 按用户 ID 改职务 / 按账号密码分配职务 |
| 业务 | `GET/POST /api/{bases,batches,readings,trace-events,products,demands,tasks,partners}`、`GET/PUT/DELETE /api/{...}/:id` | 通用 CRUD，仅限本账号数据 |
| 预警 | `POST /api/alerts/:id/ack` | 处理预警 |
| 设备 | `POST /api/devices`、`GET /api/devices/:id/secret`、`GET /api/devices/:id/series`、`POST /api/ingest/readings` | 接入设备 / 查看密钥 / 历史曲线 / 硬件上报 |
| AI | `POST /api/ai/ask`、`POST /api/ai/suggest-priority`、`GET /api/ai/status` | 问答 / 任务优先级建议 / AI 配置状态 |
| 溯源 | `GET /api/trace/:code` | 公开溯源查询 |

统一返回 `{ code, message, data }`；未登录 401、无权限 403、冲突 409。

---

## 七、快速上手（3 分钟）

1. 启动后台：`cd 后台 && npm install && npm start` → http://localhost:3000
2. 初始化演示数据（可选）：`npm run seed:demo`，再 `npm run make:admin -- demo` 把 demo 提升为管理员
3. 打开 http://localhost:3000，用 `demo / demo123456` 登录
4. 点导航栏「职位分配」给其他账号分配职务；或到各页面体验录入 → 溯源 → 市场的完整链路
5. 小程序端：`cd gushi-miniapp && npm install && npm run dev:weapp`，微信开发者工具导入（勾选“不校验合法域名”）

---

## 八、设计原则

- **数据真实性**：只展示数据库已保存的记录，无数据显示空状态，统计与库内一致，不做虚构兜底；演示数据带“演示”标记
- **安全**：scrypt 加盐哈希存储密码、JWT 会话可吊销、RBAC 后端强制校验、账号数据隔离
- **可追溯**：以批次为主线串起生产、监测、预警与销售，消费端扫码可查全过程
