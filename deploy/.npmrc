# 部署环境（服务器 / Cloud Studio）安装依赖时使用的镜像源。
#
# 为什么要固化这几行：better-sqlite3 是原生模块，npm 会先尝试下载预编译二进制，
# 失败则回退到 node-gyp 现场编译——而编译又需要下载 node headers。
# 服务器上只要 github.com / nodejs.org 有一个不通，安装就会失败。
# 把镜像写进项目文件，换机器、重装环境都不需要再手工配置。
#
# 统一改用 npmmirror：此前用的 mirrors.tencent.com/npm 镜像不完整，
# ws 等包的 tarball 会返回 404，直接导致部署时 npm install 失败。
registry=https://registry.npmmirror.com/
disturl=https://registry.npmmirror.com/-/binary/node/
better_sqlite3_binary_host=https://registry.npmmirror.com/-/binary/better-sqlite3
